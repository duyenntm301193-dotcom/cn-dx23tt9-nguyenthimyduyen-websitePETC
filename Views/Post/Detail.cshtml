@model dynamic

<div class="container mt-4">

    <h2>@Model.Title</h2>

    <img src="@Model.ImageUrl" style="width:100%; max-height:400px; object-fit:cover;" />

    <p class="mt-3">@Model.Content</p>

</div>